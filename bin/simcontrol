#!/usr/bin/env ruby

require "thor"

class SimControlCLI < Thor
  desc "init", "creates scaffolding for SimControl in this directory"
  def init
    SimControl::Init.createScaffolding(Dir.pwd)
  end
end

SimControlCLI.start(ARGV)
